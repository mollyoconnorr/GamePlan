package com.carroll.gameplan.model;

/**
 * Options for determining which day appears first in the calendar view.
 */
public enum CalendarFirstDay {
    /**
     * Start from the beginning of the configured week.
     */
    WEEK,

    /**
     * Start from the current date.
     */
    CURR_DAY
}
